import Router from './router';
import Store from './store';
declare var _default: {
    StyleSheet: (sheet: object) => (...keys: any[]) => {};
    Router: typeof Router;
    Store: typeof Store;
    createApp: <T>(router: Router<T>, store: Store<T>) => void;
};
export default _default;
